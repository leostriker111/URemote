name: ci
on: [push, pull_request]
jobs:
  selftest:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: python -m uremote selftest
      - run: pip install . && uremote perfiles
